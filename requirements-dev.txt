# Test-only dependencies. NOT installed by setup.sh — the runtime stack in
# requirements.txt is what a fork needs to actually use the assistant.
#   uv pip install --python .venv/bin/python -r requirements-dev.txt
pytest==9.1.1
httpx2==2.12.0    # starlette's TestClient backend (plain httpx is deprecated there)
