# generated artifacts (STL / STEP / G-code)
out/

# raw scan data (large, binary)
scans/raw/

# local config (printer LAN address, bridge subscription token) — keep *.example only
config/*.env
!config/*.env.example

# claude code local permission state
.claude/settings.local.json

# python
__pycache__/
*.pyc
.venv/
venv/

# os
.DS_Store
